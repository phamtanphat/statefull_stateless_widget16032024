import 'package:flutter/material.dart';

class DemoStateLessWidgetPage extends StatelessWidget {
  DemoStateLessWidgetPage();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Demo stateless widget"),
      ),
      body: Container(),
    );
  }
}
